
/**
 *transforma la primera palabra en mayuscula
 *
 * @export
 * @param {string} cadena
 * @return {*}  {string}
 */
export function oneFirsUppercase(cadena: string): string {
    return cadena.charAt(0).toUpperCase() + cadena.slice(1);
}

 
  