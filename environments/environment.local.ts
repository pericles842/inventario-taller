import { configuration } from "./TypeEnvironments";

export const environments: { host: string, configuration: configuration } = {
    host: "http://54.162.166.247:8000/",
    configuration: 'local'
}